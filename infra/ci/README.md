# GABON BIZ — CI/CD Configuration

# Placeholder — will be populated during CI/CD setup (P5-03)

# This directory will contain:

# - .gitlab-ci.yml (lint → test → build → security → deploy)

# - Dockerfile templates for microservices

# - Helm values for each environment (dev, staging, prod)
